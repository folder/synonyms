'use strict';

const { synonyms } = require('.');

// synonyms('Suggest names for a library that gets answers from an AI/LLM chatbot', {
// synonyms('Node.js library for prompting AI/LLMs', {
//   split: true,
//   // synonyms('discussion', {
//   openai: {
//     model: 'gpt-4-0613',
//     criteria: 'Half of your suggestions should be new, invented words that are simple, descriptive, creative, and unique.\nSuggestions: 25'
//   }
// })
//   .then(console.log);

// synonyms('Suggest names for a library that gets answers from an AI/LLM chatbot', {
synonyms('clean')
  .then(console.log);
